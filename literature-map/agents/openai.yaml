interface:
  display_name: "Literature Map"
  short_description: "Map related work, novelty boundaries, and closest baselines."
  default_prompt: "Use $literature-map to position this work against the most relevant papers."
policy:
  allow_implicit_invocation: true
