interface:
  display_name: "Figure Studio"
  short_description: "Plan figures and tables that carry evidentiary weight."
  default_prompt: "Use $figure-studio to storyboard the key figures and tables for this paper."
policy:
  allow_implicit_invocation: true
