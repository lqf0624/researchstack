interface:
  display_name: "Researchstack: Experiment Design"
  short_description: "Design claims, baselines, ablations, and evaluation rigor."
  default_prompt: "Use $researchstack-experiment-design to build the evaluation matrix for this paper."
policy:
  allow_implicit_invocation: true
